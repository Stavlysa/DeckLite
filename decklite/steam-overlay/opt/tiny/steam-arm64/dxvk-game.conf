# DeckLite system-wide D3D9 compatibility/performance defaults.
# Disable inherited synchronization and keep frame latency low. Turnip's
# checkerboard workaround is separate, in decklite-game-performance.sh.
d3d9.presentInterval = 0
d3d9.maxFrameLatency = 1
